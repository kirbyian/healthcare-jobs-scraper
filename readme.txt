# Required Libraries
pip install python-dotenv
pip install --upgrade beautifulsoup4
pip install openpyxl

This project scrapes specific healthcare positions from popular Irish job sites, 
extract key details, stores them in a csv and stores them in a database.
